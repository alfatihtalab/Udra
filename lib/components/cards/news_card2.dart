import 'package:flutter/material.dart';

class NewsCard2 extends StatelessWidget {
  const NewsCard2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Container(),
    );
  }
}
